# Configuración de Variables de Entorno - Le Brunch App
# Copiar este archivo como .env y actualizar con tus valores reales

# ========================================
# CONFIGURACIÓN DEL SERVIDOR NODE.JS
# ========================================

# IP del servidor (OPCIONAL - se auto-detecta si se deja vacío)
# Solo cambiar si necesitas una IP específica
NODE_SERVER_IP=

# Puerto del servidor
NODE_SERVER_PORT=3000

# Hostname alternativo (para servidor)
SERVER_HOST=

# ========================================
# BASE DE DATOS MYSQL
# ========================================

DB_HOST=localhost
DB_PORT=3306
DB_NAME=le_brunch_db
DB_USER=root
DB_PASSWORD=

# ========================================
# GOOGLE GEMINI API
# ========================================

# Claves API de Gemini (obtener de https://makersuite.google.com/app/apikey)
GEMINI_API_KEY_1=tu_clave_api_gemini_1
GEMINI_API_KEY_2=tu_clave_api_gemini_2
GEMINI_API_KEY_3=tu_clave_api_gemini_3

# ========================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ========================================

# Entorno (development/production)
NODE_ENV=development

# Dominios permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=*

# Tamaño máximo de archivo (en bytes)
MAX_FILE_SIZE=10485760

# ========================================
# INSTRUCCIONES DE USO
# ========================================

# 1. SERVIDOR:
#    - La IP se detecta automáticamente
#    - Solo cambiar NODE_SERVER_IP si necesitas una IP específica
#    - Reiniciar servidor después de cambios

# 2. FLUTTER APP:
#    - La app detectará automáticamente la IP del servidor
#    - No necesitas cambiar nada en el código Flutter
#    - Si la detección falla, puedes configurar manualmente en la app

# 3. CAMBIO DE COMPUTADORA:
#    - Solo copia este archivo .env
#    - Actualiza las claves API si es necesario
#    - ¡La IP se configurará automáticamente!

# ========================================
# NOTAS IMPORTANTES
# ========================================

# - NO subir este archivo a control de versiones (Git)
# - Mantener seguras las claves API
# - La auto-detección funciona en redes locales (192.168.x.x, 10.x.x.x)
# - En caso de problemas, verificar firewall y permisos de red 